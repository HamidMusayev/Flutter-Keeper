import 'package:flutter/material.dart';

class AppRadiuses {
  AppRadiuses._();

  static const r4 = BorderRadius.all(Radius.circular(4));
  static const r8 = BorderRadius.all(Radius.circular(8));
  static const r18 = BorderRadius.all(Radius.circular(18));
  static const r25 = BorderRadius.all(Radius.circular(25));
  static const rNavigationBar = BorderRadius.vertical(top: Radius.circular(18));
}
